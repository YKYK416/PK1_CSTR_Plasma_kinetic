# 等离子体辅助合成氨：NH 生成载体重审与机制状态图研究计划

## 0. 计划摘要

本研究聚焦于大气压 N2/H2 DBD 等离子体中 NH 初始生成的真正控制通道。核心不是泛泛地证明“振动激发有利于合成氨”，而是回答一个可被反证的机制问题：在给定气体温度、还原电场、脉冲波形与状态寿命假设下，低振动态 H2、电子激发态 H2、激发态氮原子及三体过程分别对 NH 生成和最终 NH3 产量贡献多少？

前期对 Hong 型机理输入文件的审计表明：该机制对 `N + H2(v1–v3) -> NH + H` 保留较高有效势垒，而对具名电子态 H2 与 `H2(RYDBERG_SUM)` 赋予近似无势垒的反应速率。因此，300 K 下 NH 约 86% 来自 H2* 的结果既可能包含真实的非平衡化学信息，也可能受低振动态截断、电子态 lumping 和 Rydberg 态损失网络不完整的共同影响。本计划将这一区分转化为一套可复现、可量化、可发表的机制检验框架。

研究采用零维 ZDPlasKin 气相动力学为主线；若保留表面反应，只作为敏感性扩展，不将纯气相结果表述为“催化剂促进效应”。论文主叙事为：**NH 生成的载体不是预设结论，而是受状态表示、时间尺度和电子动力学共同决定的可检验机制。**

---

## 1. 核心科学问题与假设

### 1.1 主问题

在 DBD 微放电—余辉条件下，NH 的初始生成由哪类氢载体主导；这种主导性是否会随气体温度、脉冲参数和电子动力学输入发生可解释的转换？

### 1.2 子问题

1. 对当前机理中显式包含的 H2(v1–v3)，`N + H2(v) -> NH + H` 在 300–800 K 是否能与电子激发态 H2 通道竞争？
2. 当 NH 的来源拆分为 H2(v)、具名电子态 H2、Rydberg 汇总态、N(2D)、N(2P) 与三体反应时，哪一类通道控制瞬时和周期积分 NH 通量？
3. 电子态 H2 的主导性是否对其猝灭/壁损失、Rydberg 汇总态寿命和电子碰撞截面数据库选择稳健？
4. 通过方波脉冲的频率、占空比和高场 E/N 调控，能否利用不同态的形成与衰减时间尺度改变 NH 源项结构，并将变化传递至净 NH3 产量和单位能耗？
5. 温度、脉冲和电子动力学不确定性中，哪一个因素真正决定机制结论，哪一个只改变数值幅度？

### 1.3 可检验假设

- **H1：低振动态不交叉。** 在原始 v1–v3 截断机制中，300–800 K 内 `N + H2(v1–v3)` 的速率系数不足以与 H2* 通道竞争；若无极端的 `[H2(v)]/[H2*]` 数密度比，温度扫描不会出现真正的“振动—电子态”交叉。
- **H2：电子态主导存在结构敏感性。** 若对 `H2(RYDBERG_SUM)` 引入合理寿命/猝灭范围，或将其从 NH 生成反应中关断，H2* 通道份额可能显著下降，表明原始结论受 lumped-state 表示影响。
- **H3：N(2D) 是不可忽略的第三竞争者。** 即使 H2(v) 始终较弱，`N(2D) + H2 -> NH + H` 仍可能成为重要甚至主要的 NH 源，因此任何二元比较都不足以支持机制结论。
- **H4：脉冲是机制扰动，而不仅是优化旋钮。** 当脉冲关断时间与电子态、振动态和 N/N(2D) 的特征寿命处于相近量级时，NH 通道比例会发生系统性变化；这可用于辨认通道的“记忆时间”。
- **H5：机制结论需跨碰撞组保持方向一致。** 不同电子碰撞截面组可能改变 Te、电子激发速率和 NH3 绝对产量；但若 H2* 主导或 N(2D) 主导只在某一数据库下成立，则其应被报告为模型条件结论，而非普适物理事实。

---

## 2. 基线机制与必须先完成的审计

### 2.1 当前输入文件的关键事实

基线机理文件为：

- `F:/YK_mid_sci/PK1_ZDPlaskin_vibration/2.simulation/1.one/kinet.inp`

已确认的 NH 直接生成通道包括：

| 通道类别 | 代表反应 | 当前机理含义 |
|---|---|---|
| 低振动态 H2 | `N + H2(v1–v3) -> NH + H` | 仅包含 v1–v3，且保留较高有效热势垒 |
| 电子态 H2 | `N + H2(B3SIG/B1SIG/C3PI/A3SIG) -> NH + H` | 采用近似无势垒的速率表达式 |
| Rydberg 汇总态 | `N + H2(RYDBERG_SUM) -> NH + H` | 同样采用近似无势垒速率，但损失网络需要重点核查 |
| 激发态氮 | `N(2D) + H2 -> NH + H`；`N(2P) + H2 -> NH + H` | 势垒较低，应单独统计 |
| 三体通道 | `H + N + M -> NH + M` | 大气压下可能参与 NH 生成 |

当前低振动态通道的速率形式为：

```text
N + H2(v1) -> H + NH : 4.0e-10 (Tgas/300)^0.5 exp[-16600/Tgas + 0.3×5800/Tgas]
N + H2(v2) -> H + NH : 4.0e-10 (Tgas/300)^0.5 exp[-16600/Tgas + 0.3×11600/Tgas]
N + H2(v3) -> H + NH : 4.0e-10 (Tgas/300)^0.5 exp[-16600/Tgas + 0.3×17400/Tgas]
```

相对地，电子态 H2 采用 `4.0e-10 (Tgas/300)^0.5 cm3 s-1` 的无指数势垒表达式。由此可得，在 300 K 时 v3 的速率系数约为电子态通道的 `3.4×10^-17`，在 800 K 时仍仅约为 `6.6×10^-7`。因此，本计划将“振动态 H2”严格限定为当前模型的低振动态 H2(v1–v3)，直至完成高振动态扩展。

### 2.2 基线审计清单

在批量扫描之前，必须完成下列只读审计和最小验证：

1. 确认 Hong 2017 机理及其 2018 corrigendum 的修改已被正确纳入当前文件。
2. 检查 BOLSIG+ 日志，确认 `H2(RYDBERG_SUM)` 对应电子激发过程是否被实际读取和求值。
3. 枚举每个 H2 电子态的产生、电子去激发、碰撞猝灭、壁损失、表面损失和化学损失反应。
4. 特别核查 `H2(RYDBERG_SUM)`：当前文件中需确认是否缺少与具名电子态等价的壁弛豫或重粒子猝灭通道。
5. 列出 H2(v1–v3) 的电子产生、V–V、V–T、壁弛豫与表面消耗通道，避免将低振动态稀少简单归因于反应速率。
6. 确认每个反应的单位、第三体定义、温度变量和状态能量定义；保留原始文件的不可修改副本和哈希值。

### 2.3 基线输出要求

每个基线算例必须保存：

- 全部物种的时间历程；
- 反应级 rate-of-production（ROP）或可由物种/反应速率重建的时间序列；
- 每个脉冲周期内的积分反应通量；
- E/N、Te、电子密度、输入功率/功率密度；
- NH、NH2、NH3 的净生成和消耗项；
- 初始条件、网格/积分器设置、碰撞组版本和机理文件版本。

---

## 3. 通道定义、核心指标与判据

### 3.1 NH 源项分类

将所有生成 NH 的反应归入以下互斥类别：

1. **Vib-H2：** `N + H2(v1)`, `N + H2(v2)`, `N + H2(v3)`；高振动态扩展后继续纳入该组。
2. **Named-H2*：** `N + H2(B3SIG/B1SIG/C3PI/A3SIG)`。
3. **Rydberg-H2*：** `N + H2(RYDBERG_SUM)`，必须单列，不能与具名电子态混合。
4. **Excited-N：** `N(2D) + H2` 与 `N(2P) + H2`。
5. **Association：** `H + N + M -> NH + M` 及其他明确生成 NH 的结合反应。
6. **Secondary-NH：** 由 NH3/NH2 离子或中性转化再生成 NH 的反应；该类需和“从 N2/H2 首次形成 NH”的通道区别报告。

### 3.2 定量指标

对通道 j，定义瞬时 NH 生成通量：

\[
F_j(t)=\sum_{r\in j}R_r(t)
\]

其中 \(R_r(t)\) 为反应 r 的体积反应速率。对分析窗口 \([t_0,t_1]\)，定义积分通量与份额：

\[
\Phi_j=\int_{t_0}^{t_1}F_j(t)\,dt, \qquad
f_j=\frac{\Phi_j}{\sum_k\Phi_k}.
\]

同时报告：

- NH 净生成量与各损失通量；
- NH2 和 NH3 的周期积分净生成量；
- 每单位输入能量的 NH3 产量/能量效率；
- 最大瞬时份额、周期平均份额和全程累计份额；
- 微放电高场阶段与余辉阶段的分段份额；
- 通道关断后的相对响应 \(\Delta Y_{NH3}/Y_{NH3}\) 与 \(\Delta\Phi_{NH}/\Phi_{NH}\)。

### 3.3 机制判据

- **通量主导：** 若某类通道 \(f_j \geq 0.50\)，定义为该窗口 NH 生成的主导通道。
- **竞争：** 若两类通道份额均在 0.20–0.50，定义为竞争区。
- **交叉：** 在相邻控制参数点中，两个类别的积分份额排序互换，且差异超过数值重复性/不确定性带，定义为机制交叉。
- **因果重要：** 关断或缩放某通道后，净 NH3 产量变化达到预先规定阈值（建议 10%），且变化方向在主要碰撞组中一致。
- **稳健结论：** 通道排序在预设不确定性集合中保持一致；否则报告为“条件依赖”或“模型结构敏感”。

---

## 4. 计算工作包 A：温度与状态载体机制图

### 4.1 目的

建立 \(T_g\)–\(E/N\) 平面上的 NH 生成机制图，明确低振动态 H2、电子态 H2 和激发态 N 的相对贡献，并回答当前模型是否存在 300–800 K 的“开关温度”。

### 4.2 扫描设计

| 参数 | 建议范围 | 初始分辨率 | 说明 |
|---|---:|---:|---|
| 气体温度 \(T_g\) | 300–800 K | 50 K | 在通道份额快速变化处加密至 25 K |
| 高场 E/N | 依据基线工况设置 | 至少 3–5 个代表点 | 覆盖低、中、高电子激发强度 |
| 压力 | 基线压力，通常 1 bar | 固定 | 首轮避免与温度耦合扩张 |
| N2/H2 比 | 基线组成 | 固定；后续 2–3 点验证 | 先建立清晰主效应 |
| 输入功率或功率密度 | 基线值 | 固定或闭环匹配 | 必须说明是固定 E/N 还是固定功率比较 |

### 4.3 运行分层

1. **A0：原始机理基线。** 不改变任何反应，仅完成温度扫描和通道积分。
2. **A1：Rydberg 敏感性。** 对 `H2(RYDBERG_SUM)` 的损失率施加对数尺度扫描，至少覆盖原始值、10 倍、100 倍和 1000 倍有效猝灭；另做“禁止其直接生成 NH”的诊断算例。
3. **A2：具名电子态拆分。** 分别报告 B3SIG、B1SIG、C3PI、A3SIG 对 NH 的贡献，避免将所有电子态合并为 H2*。
4. **A3：激发态 N 审计。** 关断/缩放 N(2D)+H2、N(2P)+H2，确认其对 NH 与 NH3 的因果影响。
5. **A4：高振动态扩展准备。** 在不改变研究问题的前提下，建立 v4+ 的机制扩展需求清单；只有获得可追溯的状态分辨反应和弛豫数据后才实施。

### 4.4 预期判别结果

- 若 Vib-H2 在所有 \(T_g\) 下均远低于 H2* 和 N(2D)，结论应为：**当前低振动态机制不支持 300–800 K 的振动态开启。**
- 若仅在削弱 Rydberg 寿命后 H2* 主导性消失，结论应为：**原始电子态主导依赖 lumped Rydberg 表示，须报告不确定性而非作强物理断言。**
- 若 N(2D) 在多数工况下占优，则研究焦点从“氢载体之争”提升为：**N 激发态与 H2 内部激发的协同竞争。**
- 若出现清晰通道交叉，则计算交叉曲线、对应态密度比和对净 NH3 的传递效率，并将其作为论文的核心发现。

---

## 5. 计算工作包 B：脉冲作为机制扰动

### 5.1 目的

建立频率 × 占空比 × 高场 E/N 的通道贡献图，不将脉冲仅视为工程优化，而将其视为区分短寿命电子态、较慢振动态、N/N(2D) 及余辉 NHx 化学的实验性计算探针。

### 5.2 方波 E/N(t) 实现原则

在主程序中引入明确的分段函数：

\[
E/N(t)=
\begin{cases}
(E/N)_{on}, & 0\leq t\bmod P < D P\\
(E/N)_{off}, & D P\leq t\bmod P < P
\end{cases}
\]

其中 \(P=1/f\)，\(D\) 为占空比。实现时必须：

- 保留原始连续放电版本作为可复现实验基线；
- 在每个周期边界存储所有物种和反应通量；
- 使用足以解析高场激发与早期余辉的时间步长；
- 将“固定峰值 E/N”和“固定周期平均功率”作为两种不同对照，禁止混用解释；
- 先在少量代表性工况完成数值稳定性和功率闭合检查，再展开全参数扫描。

### 5.3 参数矩阵

| 参数 | 建议水平 | 目的 |
|---|---|---|
| 频率 f | 0.1、0.5、1、5、10 kHz | 比较余辉是否充分发展 |
| 占空比 D | 5%、10%、20%、40%、70%、100% | 改变高场激发与余辉时间比例 |
| \((E/N)_{on}\) | 基线周围 3–5 点 | 控制电子激发、离解和电子温度 |
| \((E/N)_{off}\) | 0 或余辉代表值 | 明确关断后化学是否继续 |
| \(T_g\) | 300 K、机制交叉候选温度、800 K | 将脉冲与温度机制图连接 |

为控制算力，先执行 3 × 4 × 3 的筛选矩阵；仅对显示出通道排序改变或 NH3 能量效率提升的区域加密扫描。

### 5.4 脉冲分析指标

1. 高场阶段和余辉阶段各通道的 \(\Phi_j\) 与 \(f_j\)。
2. 每类关键物种的 rise time、decay time 和跨周期残余比例。
3. NH、NH2、NH3 在一个周期内的相位滞后关系。
4. 单位脉冲能量的 NH3 增量与单位能量的 NH 初始生成量。
5. 通道份额变化是否来自态密度变化、电子速率系数变化，或 NH 损失路径变化。

### 5.5 成功判据

脉冲部分的成功不要求一定发现比连续放电更高的 NH3 产率。若脉冲仅改变 H2*、N(2D) 和 Vib-H2 的相对贡献而没有提高总产量，仍可作为揭示状态寿命和余辉机制的有效结果。只有在固定功率或等能量基准下的比较中，才可声明“能效改善”。

---

## 6. 计算工作包 C：电子动力学与关键反应不确定性量化

### 6.1 目的

量化电子碰撞数据库和关键反应率不确定性对 Te、H2*、N(2D)、NH 及 NH3 预测的影响，并将不确定性带纳入机制结论。

### 6.2 不确定性来源

| 类别 | 待比较对象 | 主要影响 |
|---|---|---|
| 电子碰撞截面 | 可获得的 SIGLO、Phelps、Biagi、IST-Lisbon 或其他可追溯组 | EEDF、Te、H2/N2 激发与离解速率 |
| H2* 激发 | 电子激发到各具名态与 Rydberg 汇总态的截面 | H2* 生成速率 |
| H2* 损失 | 壁损失、重粒子猝灭、电子去激发 | 有效寿命与 NH 通量 |
| Vib-H2 反应 | `N + H2(v) -> NH + H` 的势垒降低参数 | 振动通道的温度响应 |
| N(2D) 化学 | `N(2D)+H2` 与 N(2D) 猝灭反应 | 激发态氮通道 |

### 6.3 分层 UQ 策略

1. **碰撞组离散比较：** 对每个可用截面组运行同一组代表性工况。
2. **局部对数敏感性：** 对关键速率常数单独乘以 0.1、0.3、1、3、10。
3. **组合不确定性采样：** 对最敏感的 5–10 个参数使用 Latin hypercube 或 Monte Carlo 采样；计算 NH3 产量、各 \(f_j\) 和机制分类的分布。
4. **稳健性判读：** 报告“通道份额均值 ± 置信区间”与“主导类别出现概率”，而不是只报告单一最优机制。

### 6.4 重点输出

- Te、ne、H2*、N(2D)、NH、NH2、NH3 的不确定性带；
- 各不确定参数对 \(f_{Vib-H2}\)、\(f_{H2*}\)、\(f_{N(2D)}\) 的灵敏度排名；
- 机制结论的稳健性矩阵；
- “绝对 NH3 产量不稳健但通道排序稳健”与“通道排序本身不稳健”的明确区分。

---

## 7. 建议的图表与论文结果结构

### 7.1 主文图

| 图号 | 内容 | 回答的问题 |
|---|---|---|
| Figure 1 | 研究框架：电子、振动、N(2D) 与 NHx 路径及时间尺度 | 本研究为何不能只比较单一反应？ |
| Figure 2 | 基线机理审计：关键反应、状态定义和 Rydberg 损失网络 | 86% H2* 结果可能受哪些结构假设影响？ |
| Figure 3 | 300–800 K 的状态分辨 NH 生成速率系数与所需态密度比 | 低振动态是否在该温区具备直接竞争力？ |
| Figure 4 | \(T_g\)–E/N 平面上的 NH 源项机制图 | 哪种通道在何种条件下主导？ |
| Figure 5 | Rydberg 寿命/猝灭敏感性下的 NH 来源变化 | H2* 主导是否稳健？ |
| Figure 6 | 一个脉冲周期内 E/N、H2*、H2(v)、N(2D)、NH 与 NH3 的时间轨迹 | 微放电与余辉如何分工？ |
| Figure 7 | 频率 × 占空比的通道份额和 NH3 能效地图 | 脉冲能否调控机制而非只改变总产量？ |
| Figure 8 | 跨碰撞组/UQ 的机制稳健性矩阵 | 结论受哪些输入不确定性限制？ |

### 7.2 补充信息

- 完整反应清单与分类规则；
- 基线及改进机制的逐项差异；
- 所有温度点的 ROP 表；
- 数值收敛、周期稳态判断和功率闭合；
- 每个碰撞组的 EEDF/Te 对比；
- 所有敏感性扫描的原始结果和可复现配置。

### 7.3 推荐结果叙事顺序

1. 原始机理在速率系数层面为何偏向电子态 H2；
2. 温度扫描显示低振动态 H2 是否真的“开启”；
3. 将 N(2D) 纳入后，NH 生成载体如何重新排序；
4. Rydberg 和电子态损失过程如何改变结论；
5. 脉冲如何作为时间尺度探针改变通道贡献；
6. UQ 后哪些结论仍成立；
7. 对 DBD 等离子体辅助合成氨的机制含义与模型构建建议。

---

## 8. 最小机制修改策略

为避免在未验证前引入大规模新反应网络，机制修改遵循“诊断优先、扩展随后”的原则。

### 8.1 第一阶段：诊断性开关，不宣称新机理

- 关断 `H2(RYDBERG_SUM) -> NH`；
- 对 Rydberg 汇总态添加参数化一阶损失，仅用于寿命敏感性；
- 单独关断具名 H2*、Vib-H2、N(2D) 路径；
- 对关键反应率做对数缩放。

这些算例的目的仅是识别杠杆反应和结构依赖，不应被表述为已经建立了最终物理机理。

### 8.2 第二阶段：可溯源的机理扩展

仅在取得可靠文献或数据库支持时，考虑：

- 将 H2 振动态从 v1–v3 扩展到更高 v；
- 引入状态分辨的 `N + H2(v)` 反应速率；
- 加入与反应器压力、混合气体和壁面材料相匹配的 H2* 重粒子猝灭；
- 将 Rydberg 汇总态拆分，或将其定义为具有明确有效寿命的伪物种。

每项扩展都必须记录来源、适用温压范围、单位转换和对结果的影响，且与原始机理分支隔离保存。

---

## 9. 验证、风险与边界

### 9.1 数值验证

1. 对连续基线算例复现已知的物种/产物趋势后再修改脉冲逻辑。
2. 对每个脉冲工况检查周期稳态：连续多个周期的关键物种与周期积分通量变化低于预设容差。
3. 改变最大步长、相对容差和模拟周期数，确认通道排序不受数值设置支配。
4. 检查元素守恒、电荷守恒及输入能量—功率积分的一致性。
5. 区分反应净速率、正向速率和周期积分通量，避免用净速率掩盖快速正逆循环。

### 9.2 科学风险与应对

| 风险 | 含义 | 应对措施 |
|---|---|---|
| 未出现温度交叉 | 候选 1 的直观“开关温度”不成立 | 将结果表述为低振动态在该温区不可能开启，并转向状态截断与高-v 扩展需求 |
| H2* 主导完全由 Rydberg 态决定 | 基线 86% 缺乏物理稳健性 | 将其升级为机制结构审计的核心结果，并以寿命敏感性界定结论 |
| N(2D) 始终占优 | H2(v) vs H2* 叙事不足 | 重构为 N 激发态与 H2 内部激发竞争的三载体机制图 |
| 脉冲无能效提升 | 工程优化亮点不足 | 保留脉冲作为机制扰动；强调时间分辨通道证据，而非强行宣称最优工况 |
| 截面组差异很大 | 绝对预测不可靠 | 报告不确定性带和稳健机制区，而不是单点预测 |
| 纯气相无法说明催化贡献 | “plasma catalysis”表述过度 | 主文称 plasma-assisted ammonia chemistry；表面反应另列扩展或实验验证 |

### 9.3 发表边界

- 不声称本研究已经证明真实反应器中某一态“唯一主导”；应使用“在该状态分辨模型及参数范围内”的限定。
- 不将 v1–v3 的结论外推为所有高振动态 H2 的结论。
- 不将单一碰撞组的结果作为普适机制。
- 在无实验校准时，不将预测的绝对 NH3 产率作为主要创新点；主创新应是机制图、因果通道分解和稳健性边界。

---

## 10. 阶段交付物与执行顺序

| 阶段 | 核心任务 | 完成判据 | 交付物 |
|---|---|---|---|
| P0 | 输入文件、corrigendum、BOLSIG 与状态损失网络审计 | 关键反应和缺失/不确定过程全部编号 | 机制审计表、反应分类表 |
| P1 | 连续放电基线复现与 ROP 输出 | NH/NH3 趋势及通量可重建 | 基线数据包、Figure 2 草图 |
| P2 | 温度 × E/N 状态载体扫描 | 得到机制图和交叉/无交叉判定 | Figure 3–4 |
| P3 | Rydberg/电子态/N(2D) 诊断性敏感性 | 识别结论的高杠杆假设 | Figure 5、机制稳健性初稿 |
| P4 | 方波脉冲实现与小矩阵验证 | 功率闭合、周期稳态和时间解析通过 | 脉冲程序、Figure 6 草图 |
| P5 | 脉冲参数加密扫描 | 形成机制扰动图与能效比较 | Figure 7 |
| P6 | 截面组与关键速率 UQ | 给出机制分类概率/不确定性带 | Figure 8、补充信息 |
| P7 | 论文撰写与内部审稿 | 主结论、范围和证据一一对应 | Introduction、Methods、Results、SI 框架 |

推荐执行顺序为：**P0 → P1 → P2 → P3 → P4 → P5 → P6 → P7**。只有当 P0–P3 表明原始结论具有可讨论的结构敏感性或机制交叉线索时，才值得投入较大算力进行完整脉冲矩阵。

---

## 11. 目标论文定位与可发表贡献

### 拟定题目方向

> **When Does Vibrational Hydrogen Matter? State-Resolved and Uncertainty-Aware Kinetic Analysis of NH Formation in Atmospheric-Pressure N2/H2 Plasmas**

### 预期贡献

1. 给出 NH 生成通道的状态分辨、周期积分和因果关断三重证据，而不是单一 reaction-rate ranking。
2. 定量限定当前低振动态 H2(v1–v3) 在 300–800 K 的适用范围，避免将“振动激发”作为无边界解释。
3. 揭示电子态 H2，特别是 Rydberg 汇总态的处理对 NH 来源结论的影响，并提出可复现的模型审计方法。
4. 将温度、脉冲和电子动力学不确定性整合为统一的 NH 生成机制图。
5. 给出面向后续等离子体催化模型的建议：应如何报告状态定义、猝灭过程、电子碰撞数据来源和通道稳健性。

### 相关文献锚点

- Hong et al., *J. Phys. D* (2017), NH3 气相/表面动力学机理；及其 corrigendum (2018)。
- van ’t Veer et al., *J. Phys. Chem. C* (2020), DBD 微放电与余辉中的 NH3 机制。
- Goicoechea and Roncero, *A&A* (2022), 状态分辨的 N + H2(v) 反应性。
- Zeng et al., *ACS Sustainable Chemistry & Engineering* (2023), 脉冲等离子体合成氨与能量路径。
- Sun et al., *ACS Sustainable Chemistry & Engineering* (2025), N(2D)、E/N 和 NHx 路径的动力学分析。
- Turner, *Plasma Sources Science and Technology* (2016), 复杂等离子体化学模型的不确定性与敏感性。

---

## 12. 当前下一步

最先执行的实际工作不是修改机理，而是整理 P0/P1 所需的基线输出：ZDPlasKin 运行脚本、初始条件/功率设置、物种输出文件、反应速率或 ROP 输出格式，以及 BOLSIG+ 日志。拿到这些文件后，先建立可复现的 NH 通道积分数据管线，再决定是否以及如何对 `kinet.inp` 做最小、可回退的诊断性分支修改。
